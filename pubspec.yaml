name: tbib_toast
description: Toast Package.
version: 0.0.1
homepage: https://github.com/the-best-is-best/tbib_toast

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

